---
import { TRAMITES } from "@/utils/data.js";

const { slice } = Astro.props;
---

<div class="tramites__container">
  {
    TRAMITES.slice(0, slice).map((tramite) => (
      <div class="tramites__item">
        <img src={`/${tramite.image.src}`} alt={tramite.image.alt} />
        <a href={tramite.link.href} target="_blank" rel="noopener noreferrer">
          <h3>{tramite.link.label}</h3>
        </a>
      </div>
    ))
  }
</div>

<style>
  .tramites__container {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    color: white;

    @media (width >= 768px) {
      flex-direction: row;
      flex-wrap: wrap;
    }
  }

  .tramites__item {
    display: flex;
    flex-grow: 1;
    position: relative;
    overflow: hidden;
    min-height: 8rem;
    min-width: 24rem;
    border-radius: 0.5rem;
    background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8));
    box-shadow: var(--shadow-black-lg);

    &:active {
      translate: 0 0.25rem;
    }

    &:is(:hover, :focus-within) {
      img {
        scale: 1.1;
      }
    }

    @media (width >= 768px) {
      &:is(:hover, :focus-within) {
        img {
          filter: grayscale(0);
        }
      }
    }
  }

  img {
    position: absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: left top;
    z-index: -10;
    transition:
      scale 0.3s,
      filter 0.3s;

    @media (width >= 768px) {
      filter: grayscale(100%);
    }
  }

  a {
    align-self: stretch;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    padding: 0.5rem 1rem;
  }

  h3 {
    font-weight: 800;
    text-align: center;
    text-wrap: balance;
    font-size: 1.25rem;
    text-shadow: var(--text-shadow-black);
  }
</style>
